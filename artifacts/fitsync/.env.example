# API server origin for split deploys (Vercel frontend + Render API).
# When unset, API calls use same-origin relative /api paths (single-domain deploy).
# Example: VITE_API_BASE_URL=https://fitsync-api.onrender.com
VITE_API_BASE_URL=